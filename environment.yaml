name: styleformer
channels:
  - conda-forge

dependencies:
  - python=3.12
  - pip
  # pip selects its default torch/torchvision wheels here. For explicit
  # accelerator selection before package installation, use README.md's venv setup.
  - pip:
      - -e .[dev]
